<!DOCTYPE html>
<html lang="en">

<head>

    <%- include("../partials/head",title) %>

</head>

<body>

    <%- include("../partials/navbar") %>

    <div class="container mt-3">
        <div class="row">
            <div class="col-md-12 mt-3 text-center">
                <div class="alert alert-secondary">
                    <p class="mb-3">
                        <b><%= category.categoryName %></b> <br> Do you want to delete this category entry?
                    </p>
                    <form method="post">
                        <input type="hidden" name="slug" value="<%= category.url %>">
                        <button class="btn btn-danger" type="submit">Yes</button>
                        <a href="/admin/categories" class="btn btn-secondary">No</a>
                    </form>
                </div>
            </div>
        </div>
    </div>

</body>

</html>